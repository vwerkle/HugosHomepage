# Python cache
__pycache__/
*.pyc

# Logs
logs/error.log
logs/out.log

# All data files (contains credentials, user data, picks, etc.)
# Anchored to root so static/wc-grid/data/ and wc-grid/public/data/ are not excluded
/data/

# Virtual environment
venv/

# Sensitive credential files — never commit these
data/reservations/config.json
data/reservations/opentable_session.json
